{
  "name": "note-sharer",
  "private": true,
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "start": "node scripts/start.mjs",
    "lint": "npm --prefix frontend run lint",
    "test": "npm --prefix frontend run test",
    "format": "npm --prefix frontend run format",
    "gitcheck": "node scripts/gitcheck.mjs"
  }
}
